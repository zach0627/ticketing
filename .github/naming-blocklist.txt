# 只存在於設計文件、用來界定範圍的字眼，不得出現在對外或維運看得到的任何地方
# （設計文件 12 第 3 節）：程式碼、註解、UI 文案、錯誤訊息、設定鍵、seed 資料、
# log 事件名、雲端資源名稱、README。
#
# 這份清單刻意獨立成檔案，而不是寫在 workflow 裡：
# 寫在 workflow 裡的話，那道 grep 會掃到自己的樣板而永遠紅燈。
#
# 一行一個 regex（大小寫不敏感）。
demo
示範
簡易
